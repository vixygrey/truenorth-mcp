//! Resources layer: serves the cockpit and ontology files as `truenorth://` MCP
//! resources with change notifications.
//!
//! Each submodule is a skeleton stub for the crate scaffold (task 1). The real logic
//! lands in task 14, so the module tree matches the design (§1) up front.

pub mod cockpit;
pub mod ontology;
