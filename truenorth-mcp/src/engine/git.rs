//! Git context scoped to `skills/` and `specs/` (ports the behavior of the legacy
//! `git-context.ts`), plus the git-changed-files helper for ontology scope.
//! Skeleton stub for the crate scaffold (task 1); implemented in task 7.
