//! Tools layer: the active `truenorth_*` tools plus the ported legacy catalog tools.
//!
//! Each submodule is a skeleton stub for the crate scaffold (task 1). The real logic
//! lands in tasks 8 to 12, so the module tree matches the design (§1) up front.

pub mod catalog;
pub mod gates;
pub mod lifecycle;
pub mod ontology;
pub mod skills;
pub mod tdd;
